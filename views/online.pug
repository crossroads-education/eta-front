extends ./include/layout.old.pug

block body
    div#container-header
        img(src="/img/online/header.jpg", alt="Students studying at the MAC")
        pre.header-text.text-left(style="color: white;").
            <strong>we bring</strong>

            the <strong>tutoring</strong>

            <strong>to you</strong>
    ul.list-buttons
        if rooms.length == 0
            - for (var i = 0; i < 4; i++)
                li
                    span
                        a.closed(href="") Room Closed
                    ul.closed
        - else
            each room in rooms
                li
                    span
                        a(href=room.url, target="_blank") Room #{room.letter}
                    ul
                        each course in room.courses
                            li #{course}
